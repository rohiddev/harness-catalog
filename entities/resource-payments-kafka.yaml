apiVersion: harness.io/v1
kind: Resource
identifier: payments_kafka
name: payments-kafka
type: messaging-queue
owner: group:account/team-payments
metadata:
  title: Payments Kafka Cluster
  description: >
    Kafka cluster used by payments-service for publishing payment lifecycle
    events and consuming fraud decisions and ledger confirmations.
    SASL/SCRAM-SHA-256 auth, TLS in transit. Managed by platform team.
  annotations:
    bank.com/sysid: SYSID-00000
  tags:
    - kafka
    - messaging
    - events
    - sysid-00000
spec:
  system:
    - system:account/payments_platform
